---
name: Market Intelligence Workflow
description: generate_market_report.py v1/v2 — search map + demand chart — current state and future roadmap
type: project
originSessionId: 9d233af4-29ad-42ed-adb0-7c995c9c7f00
---
## v1 — Search Volume Map (legacy, `--mode search`)
- Pulls Tableau view `39464986` (Searches by Zip Code) → ZIP bubble map
- `vf_make` filter silently ignored (confirmed — no make column in output)
- Quarter coverage uneven (47% of ZIPs have full history) — normalized via `searches_avg_per_q`
- Tableau PAT returning 401 as of 2026-05-06; use existing CSVs or Playwright fallback
- Still functional with a saved CSV; treat as legacy

## v2 — DMA Demand Chart (`--mode demand`, default)
- Source: demand signals Market Comparison CSV from admin.cars.com (`Market VDPs`, `Market connections`, `Market vehicles`)
- Make filter actually works (real `Make` column)
- Bar chart ranked by make; click-to-drill into model breakdown
- Metric toggle: VDPs / Connections / Inventory; stock type filter
- Mode toggle in UI: "Demand Chart" ↔ "Search Map"
- Test confirmed: ecarone CSV → Dallas-Ft. Worth, 24 makes, Ford #1 at 228K VDPs

## Future v3 — VDP/Lead Geographic Map (tabled)
**Goal:** Replace the search volume ZIP bubble map with a geographic demand map — VDP or lead origin by ZIP, showing WHERE consumer demand is coming from geographically.
- Requires ZIP-level lead/VDP origin data — not yet available
- Likely source: `connections_contact_details` report on admin.cars.com (not yet scraped)
- Leaflet map infrastructure already in place in the template — ready when data is available

## Tableau Data Pull (legacy search mode)
- Fallback: Playwright → open Tableau view with URL filter params → Download > Crosstab > CSV
- URL filter pattern: `?make_name=Honda&dma_market_name=Mobile-Pensacola%20(Ft%20Walt)`
- Manual CSV fallback: save to `~/Documents/Tableau/SearchVolumeByZipCode.csv`

## GitHub Pages
- Live at `jakec86.github.io/market-intelligence-reports/`
- `index.html` auto-generated by the script on each run

## Files
- Script: `~/Documents/scripts/generate_market_report.py`
- Template: `~/Documents/templates/market_intelligence_template.html`
- Placeholders: `%%DEMAND_DATA%%`, `%%MODEL_DATA%%`, `%%DEMAND_CONFIG%%` (demand); `%%CITY_DATA%%`, `%%ZIP_DATA%%` (search)
- Reports output to `~/Documents/Reports/{dma-slug}/`
- Test CSV: `~/Documents/Tableau/ecarone_market_comparison.csv` (Dallas-Ft. Worth, 99 rows)
