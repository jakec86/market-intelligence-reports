---
name: project_tools_menu
description: ~/Documents/tools_menu.html — personal reference page listing every skill/script/published report; regenerate via build_tools_menu.py
metadata: 
  node_type: memory
  type: project
  originSessionId: 7f2adbfc-94d5-4a03-9dbb-2c38b11d4d89
---

**`~/Documents/tools_menu.html`** — a single local reference page (built 2026-07-17) that lists everything built so far: all custom slash-command skills (grouped/chip-filterable by category — Recurring Reporting, On-Demand Analysis, Orchestration & Utility), the reusable Python engines behind them, the published GitHub Pages report sites, and local `~/Documents/Reports/<folder>` directories that contain generated HTML (linked via `file://`, sorted by most recent).

Generated by **`~/Documents/scripts/build_tools_menu.py`** — re-run it after adding/renaming a skill or report folder to refresh the page; the skills list is a hand-maintained Python list in that script (kept in sync with `~/.claude/CLAUDE.md`'s skill tables), the report-folder list is scanned live from disk.

**Deliberately NOT inside `~/Documents/Reports/`** (the market-intelligence-reports git repo published to GitHub Pages) — this menu names internal workflow/client details that shouldn't go on a public page. Keep it a local-only file. This is distinct from `~/Documents/Reports/index.html`, which is the live filterable hub for *just* the Market Intelligence DMA/make reports — don't conflate the two or overwrite one with the other.

**Published sites catalogued:** `market-intelligence-reports` (main hub), `jlr-swh-performance-report` (standalone one-off, see [[project_dealer_market_report_engine]]), `mystery-shop` ([[project_mystery_shop_tool]]), `aca-mae-map` ([[project_aca_mae_proposal]]) — all under the `jakec86` personal GitHub account, confirmed via `search_repositories` rather than guessed.
